---
title: 'Structure'
description: 'Rule reference: the structure family.'
sidebar:
  order: 7
  label: 'Structure'
---

Rule kinds in the **Structure** family. Each entry below has its own page with options, an example, and any auto-fix support.

- [`max_directory_depth`](/docs/rules/structure/max_directory_depth/) — Tree depth from repo root may not exceed `max`.
- [`max_files_per_directory`](/docs/rules/structure/max_files_per_directory/) — Per-directory fanout may not exceed `max`.
- [`no_empty_files`](/docs/rules/structure/no_empty_files/) — Flag zero-byte files.
