name: file_max_size is silent when every file is under the limit
tags: [check, file_max_size, passing]

given:
  tree:
    tiny.txt: "x"
    also_tiny.txt: "hi"
  config: |
    version: 1
    rules:
      - id: under-10-bytes
        kind: file_max_size
        paths: "**/*.txt"
        max_bytes: 10
        level: warning

when: [check]

expect:
  - violations: []
