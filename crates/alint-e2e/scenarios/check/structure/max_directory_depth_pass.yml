name: max_directory_depth is silent when every path is within the cap
tags: [check, max_directory_depth, structure, passing]

docs:
  title: Every file stays within the directory-depth cap
  case: pass
  kind: max_directory_depth

given:
  tree:
    README.md: ""
    src:
      lib.rs: ""
      mod:
        inner.rs: ""
  config: |
    version: 1
    rules:
      - id: shallow
        kind: max_directory_depth
        paths: "**"
        max_depth: 3
        level: warning

when: [check]

expect:
  - violations: []
