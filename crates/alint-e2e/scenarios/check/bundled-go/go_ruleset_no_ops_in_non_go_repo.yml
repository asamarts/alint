name: alint://bundled/go - every rule is silent when facts.has_go is false
tags: [check, extends, bundled, go]

given:
  tree:
    "README.md": "# not a go project\n"
  config: |
    version: 1
    extends:
      - alint://bundled/go@v1

when: [check]

expect:
  - violations: []
