name: file_exists is silent when the required file is present
tags: [check, file_exists, passing]

given:
  tree:
    README.md: "# demo"
    Cargo.toml: "[package]\nname = \"demo\"\n"
  config: |
    version: 1
    rules:
      - id: has-readme
        kind: file_exists
        paths: README.md
        root_only: true
        level: error

when: [check]

expect:
  - violations: []
