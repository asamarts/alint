name: dir_exists is silent when the directory is present
tags: [check, dir_exists, passing]

given:
  tree:
    Cargo.toml: "[package]\nname = \"demo\"\n"
    src:
      main.rs: "fn main() {}"
  config: |
    version: 1
    rules:
      - id: has-src
        kind: dir_exists
        paths: "src"
        level: error

when: [check]

expect:
  - violations: []
